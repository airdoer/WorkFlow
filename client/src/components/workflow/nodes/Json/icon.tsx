import React from 'react';
import { FileTextOutlined } from '@ant-design/icons';

const JsonIcon: React.FC = () => <FileTextOutlined style={{ fontSize: 24, color: '#faad14' }} />;

export default JsonIcon;
